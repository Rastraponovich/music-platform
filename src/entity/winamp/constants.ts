export enum TimeMode {
  ELAPSED = "ELAPSED",
  REMAINING = "REMAINING",
}

export enum MediaStatus {
  PAUSED = "PAUSED",
  PLAYING = "PLAYING",
  STOPPED = "STOPPED",
}

export enum WinampState {
  CREATED = "CREATED",
  INIT = "INIT",
  TRACKLOADED = "TRACKLOADED",
  DESTROYED = "DESTROYED",
  CLOSED = "CLOSED",
  OPENED = "OPENED",
  MINIMIZED = "MINIMIZED",
}
